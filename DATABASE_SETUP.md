# 数据库版本使用指南

## 📊 数据库架构

本项目已集成 SQLite + Express 后端，实现数据持久化管理。

### 技术栈
- **数据库**: SQLite (better-sqlite3)
- **后端**: Express.js
- **前端**: React + Vite

## 🚀 快速开始

### 1. 安装依赖（如未安装）

```bash
npm install
```

### 2. 启动后端API服务器

```bash
npm run server
```

服务器将在 `http://localhost:3001` 启动

### 3. 启动前端开发服务器

在另一个终端窗口：

```bash
npm run dev
```

前端将在 `http://localhost:5173` 启动

### 4. 同时启动前后端

```bash
npm start
```

这将使用 `concurrently` 同时运行前端和后端。

## 📁 文件结构

```
├── server/
│   ├── index.cjs                 # Express API服务器
│   └── db/
│       ├── schema.cjs            # 数据库表结构定义
│       ├── seed.cjs              # 数据填充脚本
│       └── orthorobot.db         # SQLite数据库文件（自动生成）
├── src/
│   ├── data/                     # 原始数据文件（仍保留用于种子数据）
│   ├── components/               # React组件
│   ├── hooks/                    # 自定义Hooks
│   └── utils/                    # 工具函数
└── package.json
```

## 🗄️ 数据库表结构

### 主要表：
- `robots` - 机器人基本信息
- `indications` - 适应症
- `advantages` - 优势
- `limitations` - 局限性
- `patient_benefits` - 患者收益
- `hospitals` - 合作医院
- `implant_compatibility` - 假体兼容性
- `news` - 行业新闻
- `faqs` - 常见问题
- `patient_stories` - 患者故事
- `surgical_tips` - 手术提示
- `cities` - 城市数据
- `anatomy_education` - 解剖学教育
- `pricing_policies` - 医保政策

## 🔌 API端点

### 基础端点

#### 健康检查
```
GET /api/health
```

#### 机器人数据
```
GET /api/robots           # 获取所有机器人
GET /api/robots/:id       # 获取单个机器人
```

#### 内容数据
```
GET /api/news                    # 行业新闻
GET /api/faqs                    # 常见问题
GET /api/patient-stories        # 患者故事
GET /api/surgical-tips          # 手术提示
GET /api/cities                 # 城市数据
GET /api/anatomy-education      # 解剖学教育
GET /api/pricing-policies       # 医保政策
```

#### 选项数据
```
GET /api/indications        # 适应症选项
GET /api/camera-options     # 相机品牌选项
GET /api/arm-options        # 机械臂品牌选项
```

### 示例请求

```bash
# 获取所有机器人
curl http://localhost:3001/api/robots

# 获取健康状态
curl http://localhost:3001/api/health

# 获取新闻
curl http://localhost:3001/api/news
```

## 🛠️ 数据管理

### 重新填充数据库

如需重置数据库：

```bash
# 删除数据库文件
rm server/db/orthorobot.db

# 重启服务器（会自动重新创建并填充数据）
npm run server
```

### 直接运行种子脚本

```bash
node server/db/seed.cjs
```

## 📊 数据库查看

推荐使用以下工具查看SQLite数据库：

- **DB Browser for SQLite** - GUI工具
- **SQLite CLI** - 命令行工具
  ```bash
  sqlite3 server/db/orthorobot.db
  ```

## 🔄 从JSON迁移到数据库

**优势：**
✅ 数据持久化
✅ 高效查询
✅ 关系数据管理
✅ 易于扩展
✅ API标准化

**原始JSON数据：**
- `src/data/robotsData.js` - 9个机器人详细数据
- `src/data/contentData.js` - 新闻、FAQ等内容

这些文件仍然保留，用于数据库种子数据生成。

## 🚧 待办事项

- [ ] 修改前端从API获取数据（而非导入JSON）
- [ ] 添加数据缓存策略
- [ ] 实现数据更新API
- [ ] 添加API认证
- [ ] 优化查询性能

## 💡 注意事项

1. **首次运行**：服务器首次启动时会自动创建数据库并填充数据
2. **数据更新**：修改数据需要删除 `server/db/orthorobot.db` 并重启服务器
3. **端口占用**：确保端口 3001（后端）和 5173（前端）未被占用
4. **开发模式**：建议使用 `npm start` 同时运行前后端

## 📝 package.json 脚本

```json
{
  "dev": "vite",                              // 仅前端
  "build": "vite build",                      // 构建前端
  "server": "node server/index.cjs",          // 仅后端
  "start": "concurrently \"npm run server\" \"npm run dev\""  // 前后端同时
}
```

## 🎯 下一步

1. 运行 `npm start` 启动完整项目
2. 访问 `http://localhost:5173` 查看前端
3. API文档：`http://localhost:3001/api/health`
4. 开始开发！

---

**创建日期**: 2025-12-28
**版本**: 1.0.0
